[project]
name = "eng-id-nmt-basic"
version = "0.1.0"
description = ""
authors = [
    {name = "pnabilah",email = "puti.nabilah@ui.ac.id"}
]
readme = "README.md"
requires-python = ">=3.10, <3.15"
dependencies = [
    "tensorflow (==2.20.0)",
    "jupyter (==1.1.1)",
    "ipykernel (==7.2.0)",
    "matplotlib (==3.10.8)",
    "pandas (==2.3.3)",
    "pyqt5 (==5.15.11)"
]

[tool.poetry]
packages = [{include = "eng_id_nmt_basic", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
